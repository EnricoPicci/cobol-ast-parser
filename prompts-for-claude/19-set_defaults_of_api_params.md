Make sure that the default values for the input parameters are the following:
    resolve_copies=True,
    include_redefines=True,
    include_ancestor_mods=True,
    include_source_info=True,

The path to the copybooks by default is the same as the folder of the Cobol program. Adjust the defaults accordingly.